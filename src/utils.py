############################################################
# 📘 文件说明：
# 本文件实现的功能：通用工具函数集合，提供跨模块复用的辅助功能（如路径处理、格式化）。
#
# 📋 程序整体伪代码（中文）：
# 1. 定义静态工具函数
# 2. 处理特定单一任务（如获取路径）
# 3. 返回标准化结果
#
# 🔄 程序流程图（逻辑流）：
# ┌──────────┐
# │  输入数据 │
# └─────┬────┘
#       ↓
# ┌────────────┐
# │  核心处理逻辑 │
# └─────┬──────┘
#       ↓
# ┌──────────┐
# │  输出结果 │
# └──────────┘
#
# 📊 数据管道说明：
# 数据流向：输入参数 → 逻辑处理 → 返回值/对象状态变更
#
# 🧩 文件结构：
# - 依赖库：os, sys
# - 函数 (Function)：resource_path - (独立功能模块)
#
# 🕒 创建时间：2026-02-06
############################################################

import os
import sys

def resource_path(relative_path):
    """ 获取资源的绝对路径，兼容开发环境和 PyInstaller 打包后的环境 """
    try:
        # PyInstaller 创建临时文件夹将路径存储在 _MEIPASS 中
        base_path = sys._MEIPASS
    except Exception:
        base_path = os.path.abspath(".")

    return os.path.join(base_path, relative_path)